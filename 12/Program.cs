// Need to do path search from S to E.
// Should code an algorithm from Wikipedia / algorithms book.

foreach (string line in System.IO.File.ReadLines(@"./sample_input")) {
    Console.WriteLine(line);
}
